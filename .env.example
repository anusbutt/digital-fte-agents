# AI Employee Configuration
# Copy this file to .env and update values as needed

# Safety: When true, all actions are logged but NOT executed
# Set to false only when you're ready for live operation
DRY_RUN=true

# Vault path (defaults to current directory if not set)
# VAULT_PATH=.

# Watcher check interval in seconds
# CHECK_INTERVAL=10
